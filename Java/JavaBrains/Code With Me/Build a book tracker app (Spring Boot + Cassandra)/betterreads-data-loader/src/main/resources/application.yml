spring.application.name: betterreads-data-loader

datadump.location:
  author: useYourValueHere
  works: useYourValueHere
  
spring:
  main:
    allow-circular-references: true
  data:
    cassandra:
      keyspace-name: main
      username: useYourValueHere
      password: useYourValueHere
      schema-action: create-if-not-exists
      request:
        timeout: 10s
      connection:
        connect-timeout: 10s
        init-query-timeout: 10s
        
datastax.astra:
  secure-connect-bundle: secure-connect.zip

astra.db:
  id: useYourValueHere
  region: europe-west1
  keyspace: main
  application.token: useYourValueHere